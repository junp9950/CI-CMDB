# Azure CIDB/CMDB Web App 프로젝트

## 개요

Azure 인프라의 구성 정보(CIDB)와 변경 이력(CMDB)을 수집·저장·조회하는 Web App.  
MVP는 Azure 실제 연동 없이 동작하며, 설정 변경만으로 실제 Azure 환경에 연결 가능한 구조로 설계한다.

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | React + TypeScript |
| Backend | FastAPI (Python) |
| Database | SQLite → (추후) Azure SQL |
| Azure 인증 | App Registration (Entra ID) |
| Azure 리소스 수집 | Azure Resource Graph (MVP는 Mock) |

---

## Azure 인증 방식

- Azure Portal → Entra ID → **앱 등록**으로 앱 생성
- 구독에 **Reader 권한** 부여
- 아래 4개 값을 `.env`에 저장하여 사용

```env
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=
```

---

## 프로젝트 구조

```
azure-cidb-cmdb/
├── backend/
│   ├── app/
│   │   ├── api/              # FastAPI 라우터
│   │   ├── core/             # 설정, DB 연결
│   │   ├── models/           # SQLAlchemy 테이블 정의
│   │   ├── schemas/          # Pydantic 입출력 스키마
│   │   ├── repositories/     # DB 접근 레이어
│   │   ├── services/         # 비즈니스 로직
│   │   ├── collectors/       # Azure 리소스 수집 (인터페이스 분리)
│   │   │   ├── base.py       # ABC
│   │   │   ├── mock.py       # MVP용 샘플 데이터 반환
│   │   │   └── azure_rg.py   # 실제 Resource Graph 연동
│   │   ├── engines/
│   │   │   └── risk_engine.py  # 위험도 분석 룰 엔진
│   │   └── notifiers/        # 알림 (Teams/Slack)
│   │       ├── base.py
│   │       ├── mock.py
│   │       ├── teams.py
│   │       └── slack.py
│   ├── seed/                 # 샘플 초기 데이터
│   ├── data/                 # SQLite DB 파일 저장 위치
│   └── main.py
│
├── frontend/
│   ├── src/
│   │   ├── api/              # Axios 클라이언트
│   │   ├── types/            # TypeScript 타입
│   │   ├── hooks/            # 커스텀 훅
│   │   ├── components/       # 공통 컴포넌트
│   │   └── pages/
│   │       ├── Dashboard/
│   │       ├── Resources/    # CIDB 조회
│   │       ├── Changes/      # CMDB 조회
│   │       └── Settings/     # Webhook 설정
│
├── .env
├── .env.example
└── README.md
```

---

## DB 구조

### resources (CIDB)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | TEXT PK | UUID |
| subscription_id | TEXT | |
| resource_group | TEXT | |
| name | TEXT | 리소스 이름 |
| type | TEXT | microsoft.compute/virtualmachines 등 |
| location | TEXT | koreacentral 등 |
| sku | JSON | nullable |
| tags | JSON | |
| has_public_ip | BOOL | |
| has_private_endpoint | BOOL | |
| has_nsg | BOOL | |
| collected_at | DATETIME | 수집 시각 |

### change_events (CMDB)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | TEXT PK | UUID |
| resource_id | TEXT FK | |
| resource_name | TEXT | 스냅샷 (삭제 후 조회 대비) |
| resource_type | TEXT | |
| operation | TEXT | Create / Update / Delete |
| changed_by | TEXT | |
| changed_at | DATETIME | |
| before_state | JSON | |
| after_state | JSON | |
| diff | JSON | 변경된 필드만 |
| risk_level | TEXT | High / Medium / Low / None |
| risk_reason | TEXT | 트리거된 룰 설명 |
| notification_sent | BOOL | |

### notification_configs

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | TEXT PK | |
| name | TEXT | |
| webhook_type | TEXT | teams / slack |
| webhook_url | TEXT | |
| min_risk_level | TEXT | 최소 전송 위험도 |
| enabled | BOOL | |

---

## API 목록

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/dashboard | 대시보드 통계 |
| GET | /api/resources | 리소스 목록 조회 (필터 지원) |
| POST | /api/resources/sync | Azure에서 리소스 수집 및 저장 |
| GET | /api/changes | 변경 이력 조회 (필터 지원) |
| POST | /api/events/activity-log | 변경 이벤트 수신 |
| POST | /api/notifications/test | 알림 테스트 발송 |
| POST | /api/ai/chat | AI 자연어 질의 (2차, 뼈대만) |

---

## 위험도 분석 규칙

| 조건 | 위험도 |
|------|--------|
| NSG inbound deny 추가 | High |
| NSG allow 삭제 | High |
| UDR 변경 | High |
| Private Endpoint 삭제 | High |
| Public IP 생성 | Medium |
| VM Size 변경 | Medium |
| Tag 변경 | Low |

---

## Collector 전환 방식

MVP와 실제 Azure 연동은 `.env` 설정 하나로 전환된다.

```env
COLLECTOR_MODE=mock    # MVP 단계
COLLECTOR_MODE=azure   # 실제 Azure 연동 시
```

코드 변경 없이 Collector 구현체만 교체되는 구조.

---

## DB 전환 방식

SQLAlchemy를 사용하므로 connection string 교체만으로 DB를 변경할 수 있다.

```env
# SQLite (MVP)
DATABASE_URL=sqlite:///./data/cidb.db

# Azure SQL (운영)
DATABASE_URL=mssql+pyodbc://user:pass@server.database.windows.net/db?driver=ODBC+Driver+17+for+SQL+Server
```

---

## 구현 단계

| 단계 | 내용 |
|------|------|
| 1단계 | DB 모델 + Repository + 샘플 seed 데이터 |
| 2단계 | Service 레이어 + Risk Engine |
| 3단계 | FastAPI 엔드포인트 전체 구현 |
| 4단계 | React 화면 (Dashboard → CIDB → CMDB → Settings) |
| 5단계 | (선택) AI 자연어 질의 확장 |

---

## 설계 원칙

- MVP는 반드시 실행 가능해야 한다
- Azure 실제 연동 없이도 동작해야 한다
- 모든 Azure 연동부는 인터페이스로 분리하여 교체 가능하게 설계한다
- 인증 정보는 절대 코드에 하드코딩하지 않는다
- Webhook URL은 DB에 암호화 저장을 고려한다
